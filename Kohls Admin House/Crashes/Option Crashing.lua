local crash = freeze
if crash == freeze then
        for i = 1, 1750 do
            game.Players:Chat('freeze '..string.rep(' all', 40))
            game.Players:Chat('clone all all all')
            game.Players:Chat('removeclones')
            task.wait()
        end
end
if crash == dog then
        for i = 1, 1750 do
            game.Players:Chat('dog all all all')
            game.Players:Chat('clone all all all')
            task.wait()
        end
end
